name: CI/CD Pipeline

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: pip install fastapi uvicorn google-cloud-aiplatform
      - name: Run test
        run: python -c "import app"
